/* 
An error is a type of object built into the JS language, consisting of a name/type and a message. Errors contain crucial information that can assist you in locating the code responsible for the error, determining why you have this error, and resolving the error.
*/

/* 
TYPES OF ERRORS

Syntax Error: occurs when the code is not written properly

Reference Error: occurs when the code is referring to the variable that does not exist, or is out of scope

Type error
These errors are thrown for a few different reasons:

Per MDN, a TypeError may be thrown when:

- an operand or argument passed to a function is incompatible with the type expected by that operator or function;
- or when attempting to modify a value that cannot be changed;
- or when attempting to use a value in an inappropriate way.
*/

/**
 Tips for Debugging
 - Use console debuggger
 - Google
 - Use the console output
 - Error message is your friend

 warnins provide potential insight on problems that could crash your problem (but do not acutally crash)
 */